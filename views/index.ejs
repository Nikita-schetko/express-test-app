<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>MY APP</title>
  <link rel="stylesheet" href="main.css">
</head>
<body>
  <h1>May Node and Express be with you.</h1>

  <ul class="quotes">
  <% for(var i=0; i<quotes.length; i++) {%>
    <li class="quote" >
      <span class="name"><%= quotes[i].name %></span>
      <span class="quote"><%= quotes[i].quote %></span>
      <button type="button" data-type="edit" onclick="editQuote(this,'<%= quotes[i]._id %>')">&nbsp;</button>
      <button type="button" data-type="save" onclick="updateQuote(this,'<%= quotes[i]._id %>')" class="hidden" >Save</button> 
      <button type="button" data-type="cancel" onclick="cancelEditing(this,'<%= quotes[i]._id %>')" class="hidden" >Cancel</button>       
      <button type="button" data-type="remove" onclick="deleteQuote('<%= quotes[i]._id %>')">&nbsp;</button>
    </li>
  <% } %>
  </ul>

  <form action="/quotes" method="POST">
    <input type="text" placeholder="name" name="name">
    <input type="text" placeholder="quote" name="quote">
    <button type="submit">Submit</button>
  </form>
    <script src="main.js"></script>
</body>
</html>